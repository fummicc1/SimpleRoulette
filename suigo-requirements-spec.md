# suigo 要求仕様書

| 項目 | 内容 |
|---|---|
| 文書名 | suigo 要求仕様書 |
| 版 | 0.1 (ドラフト) |
| 作成日 | 2026-08-27 |
| ステータス | レビュー待ち — §9「設計判断」の各決定は推奨案であり、確定前にレビューを要する |

本書は OSS「suigo」— Go で UI を記述し、SwiftUI が解釈する **プロセス内 Server-Driven UI (SDUI) フレームワーク** — の要求仕様を定義する。

要求の優先度は RFC 2119 に準じて表記する:

- **MUST** — v0.1 リリースの必須条件
- **SHOULD** — v0.1 に含めることを強く推奨するが、合理的理由があれば見送り可
- **MAY** — 任意。将来バージョンの候補

---

## 1. 背景と目的

### 1.1 技術的背景

Go から SwiftUI を直接呼び出すことは実質不可能である。

- `some View` の opaque type と巨大なジェネリック型は C ABI に落とせない
- `@ViewBuilder` はコンパイル時変換であり、ランタイムでの合成ができない
- SwiftUI は値型 struct と静的解決を前提としたメタデータ構造を持つ
- Swift の `@_cdecl` は非公式属性で、渡せる型は C プリミティブに限られる(`@c` 属性の正式化は Swift コミュニティで議論中だが時期未確定)

現実解として確立しているのは「SwiftUI をホスト、Go をライブラリ」とする Tailscale iOS 方式だが、この方式では UI 層をすべて Swift で手書きする必要がある。

### 1.2 埋めるべき空白

- Go コアを持つアプリで「UI 記述まで Go に寄せたい」場合の中間解が OSS として存在しない
- Server-Driven UI の知見 (Airbnb, Yandex DivKit) は「ネットワーク越し」前提で蓄積されており、「プロセス内・言語間ブリッジ」への転用例がない
- 2026-08 時点の調査で、「**Go authoring × プロセス内 C ABI × SwiftUI インタープリタ × バイナリパッチ**」の組み合わせは空白であることを確認済み(§1.3)

### 1.3 先行・類似プロジェクトとの差分 (2026-08 時点)

| プロジェクト | 概要 | suigo との差分 |
|---|---|---|
| Govinci (GraHms/govinci) | Go 純粋の宣言的 DSL + hooks 風 state + diff + gomobile bind (⭐68) | レンダラーは Android のみ。iOS は未着手かつ UIKit 志向。シリアライズは JSON ツリー |
| DivKit (Yandex) | クロスプラットフォーム SDUI。iOS は UIKit view を生成 | ネットワーク SDUI 前提。authoring 言語に Go なし。SwiftUI ネイティブでない |
| ServerDrivenSwiftUI ほか JSON→SwiftUI 系 | Swift 側で JSON を SwiftUI view に変換 | ネットワーク前提・Swift/サーバー authoring。C ABI・Go 連携なし |
| Tailscale iOS | Go コア + SwiftUI 手書き | UI 記述は Swift。suigo はこの UI 層を Go に寄せる |
| SwiftlyGo / go-gooey / dovetail | SwiftUI 風の Go DSL | ターゲットが Web/GTK で Apple ネイティブ UI ではない |

### 1.4 プロダクトビジョン

1. Go で書かれたコアロジックを持つ iOS/macOS アプリにおいて、**UI 定義・状態管理・イベント処理を Go 側に集約**し、Swift 側は薄い解釈層のみとする。
2. UI とロジックの境界を「**C ABI に落とせる形**(プリミティブ + シリアライズ可能なデータ)」に保つことを設計原則とする。これにより将来 Swift/SwiftUI 側の技術変化が起きても Go コアが影響を受けない。
3. 将来的に Android (Jetpack Compose) インタープリタを追加し、Go 側資産を水平展開できる構造とする(v1 スコープ外)。

---

## 2. ターゲットユーザーとユースケース

### 2.1 想定ユーザー

- Go でバックエンド / CLI / ライブラリを持ち、iOS アプリ化したい個人・小規模チーム
- Tailscale 方式(Go コア + Swift UI 手書き)を採用済みで、UI の二重実装コストに悩むチーム
- 社内ツール・設定画面・ダッシュボードなど「ロジックが濃く UI が素直」なアプリの開発者

### 2.2 代表ユースケース

- UC-1: Go 製 CLI ツールの設定画面・ステータス表示を iOS アプリとして提供する
- UC-2: Go コアの状態(接続状態、同期進捗など)をリアルタイムに反映するダッシュボードを表示する
- UC-3: フォーム入力(テキスト・トグル・ピッカー)を受け取り、Go 側のバリデーション・永続化ロジックに直結させる

### 2.3 スコープ外(明示的な非ターゲット)

以下は suigo が **意図的に対応しない** 領域であり、プリミティブ追加要望に対する防波堤としてこの宣言を仕様の一部とする(§10 リスク参照)。

- リッチなアニメーション・カスタム描画が主体のアプリ(ゲーム、クリエイティブ系)
- SwiftUI の全 API 網羅(DivKit 級の肥大化を避ける)
- 既存 UIKit アプリへの部分組み込み(v1 では新規 SwiftUI ホスト前提)

---

## 3. システム構成の概要

```
┌─────────────────────────── アプリプロセス ───────────────────────────┐
│                                                                      │
│  ┌──────── Go 側 ────────┐   C ABI 境界    ┌────── Swift 側 ──────┐  │
│  │ 宣言的コンポーネントAPI │                │ SwiftUI インタープリタ │  │
│  │ 状態ストア             │ ──バイトバッファ→ │ (ノード型→View 変換)  │  │
│  │ diff / パッチ生成      │                │ パッチ適用 (差分更新)  │  │
│  │ エンコーダ            │ ←イベント CB ── │ イベント送出          │  │
│  └───────────────────────┘                └───────────────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
```

- Go 側: UI ツリーの構築・状態管理・diff 計算・パッチのエンコードを担う(pure Go)
- 境界: バイトバッファとイベントコールバック関数ポインタのみが越境する
- Swift 側: バッファをデコードし SwiftUI View にマップする薄いインタープリタ。パッチを適用して差分更新する

---

## 4. 機能要求

### 4.1 Go 側 (authoring + runtime)

| ID | 要求 | 優先度 |
|---|---|---|
| FR-G1 | 宣言的コンポーネント API により UI ツリーを構築できること。コンテナは可変長引数で子を受け取る形(例: `VStack(Text("Hello"), Button("Tap", onTap))`) | MUST |
| FR-G2 | v0.1 のプリミティブとして次の 15 種を提供すること: Text, Button, VStack, HStack, ZStack, List, TextField, Toggle, Image, Spacer, Divider, ScrollView, NavigationStack, ProgressView, Picker | MUST |
| FR-G3 | modifier 最小セットを提供すること: padding, font, foregroundColor, background, frame, cornerRadius | MUST |
| FR-G4 | Go 側に状態ストアを持ち、「イベント受信 → ツリー再構築 → Go 側で diff → パッチ生成」のサイクルを実装すること | MUST |
| FR-G5 | UI ツリーおよびパッチをバイトバッファへシリアライズできること。エンコーダは差し替え可能な抽象の背後に置くこと(形式の決定は §9-1) | MUST |
| FR-G6 | タップ・テキスト入力・トグル・リスト選択のイベントを Go の関数コールバックとして登録できること | MUST |
| FR-G7 | ツリー構築・diff・エンコードは pure Go で実装し、macOS/cgo なしで動作すること(テスト容易性 NFR-3 の前提) | MUST |

### 4.2 境界 (C ABI)

| ID | 要求 | 優先度 |
|---|---|---|
| FR-B1 | 境界を跨ぐインターフェースは「バイトバッファ」と「イベントコールバック関数ポインタ」の 2 つのみとすること | MUST |
| FR-B2 | 境界は Swift の型システム・ARC・witness table に依存しないこと | MUST |
| FR-B3 | goroutine ↔ メインスレッドのディスパッチ規約を仕様として明文化すること。UI 更新は必ずメインスレッドで行う。Go → Swift のパッチ通知は任意スレッドから発行可能とし、Swift 側でメインスレッドへディスパッチする。Swift → Go のイベントコールバックはメインスレッドから呼ばれ、Go 側で長時間処理を行う場合は goroutine へ逃がす責務を利用者が負う | MUST |
| FR-B4 | バッファの所有権・解放規約(どちら側が alloc/free するか)をヘッダコメントおよびドキュメントで明文化すること | MUST |

### 4.3 Swift 側 (interpreter)

| ID | 要求 | 優先度 |
|---|---|---|
| FR-S1 | ノード型 → SwiftUI View の変換を「switch 一枚」の薄いインタープリタとして実装すること。独自レイアウトエンジン・独自描画を持たないこと | MUST |
| FR-S2 | パッチ適用による差分更新を行い、全ツリー再構築を避けること | MUST |
| FR-S3 | 未知ノード型に対して fallback 動作をすること(空 View を表示し、デバッグビルドではログを出す)。クラッシュ・デコード全体の失敗をしないこと | MUST |
| FR-S4 | 未知の modifier・未知のイベント種別についても同様に無視 + デバッグログで前方互換を保つこと | SHOULD |

### 4.4 ツールチェーン・配布

| ID | 要求 | 優先度 |
|---|---|---|
| FR-T1 | `go build -buildmode=c-archive` → xcframework 化までを行うビルドコマンドを同梱すること(gomobile の古さを補う) | MUST |
| FR-T2 | Swift 側は SwiftPM で導入できるパッケージとして配布すること | MUST |
| FR-T3 | サンプルアプリ 3 種(カウンター、TODO、設定画面)を同梱すること | MUST |
| FR-T4 | README に Govinci・DivKit・Tailscale 方式との比較表を掲載し、立ち位置を明示すること | MUST |
| FR-T5 | README に Go ランタイム込みのバイナリサイズ増分を明記すること(Tailscale 実績を参考値とする) | MUST |

---

## 5. 非機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| NFR-1 | **パフォーマンス**: パッチ適用が 60fps を阻害しないこと。v0.1 では「体感遅延なし」の定性目標とし、計測基盤を整えた後に定量目標(例: パッチ適用 p99 レイテンシ)を設定する | MUST |
| NFR-2 | **バイナリサイズ**: Go ランタイム込みの増分を計測し README に明記すること(FR-T5) | MUST |
| NFR-3 | **テスト容易性**: Go 側(ツリー構築 + diff + エンコード)は pure Go で macOS なしにテスト可能であること。CI は Linux ランナーで Go 側テストを完結できること | MUST |
| NFR-4 | **互換性**: iOS 17+ / macOS 14+ を初期ターゲットとし、SwiftUI の安定 API 範囲のみ使用すること | MUST |
| NFR-5 | **スキーマ進化**: ノード定義の追加が Swift 側の再リリースなしに壊れないこと(FR-S3/S4 の fallback が前提)。フィールドの削除・意味変更はメジャーバージョン境界のみで許可 | MUST |
| NFR-6 | **保守性**: Swift インタープリタは「薄さ」を保守目標とする。SwiftUI API 変更への追従コストを抑えるため、Swift 側にビジネスロジック・状態を持たせない(例外は §9-4 のローカル入力 state) | SHOULD |

---

## 6. 開発の進め方に関する制約・前提

- 実装順序は次のとおりとする:
  1. Go 側ツリー構築 + diff + エンコード(pure Go、テスト先行)
  2. Swift インタープリタ
  3. ビルドツール
  4. サンプルアプリ
- Govinci を参照実装として研究する。特に reconcile/diff の設計と、iOS 実装が止まった要因を分析し、同じ轍を踏まない
- リポジトリ名 `suigo` を確保する

---

## 7. 受け入れ基準 (v0.1)

v0.1 は以下をすべて満たしたときに完了とする。

1. サンプル 3 種(カウンター / TODO / 設定画面)が iOS 実機で動作する
2. FR/NFR の MUST 項目をすべて満たす
3. Go 側パッケージ(特に diff エンジン)に単体テストがあり、Linux CI で回る
4. README に比較表(FR-T4)とバイナリサイズ増分(FR-T5)が記載されている

---

## 8. 成功指標

- **v0.1 時点**: サンプル 3 種の実機動作(受け入れ基準)
- **継続指標**: Go 側パッケージのテストカバレッジ(diff エンジン中心)
- **コミュニティ**: 外部コントリビュータによる Issue/PR の発生
- **中期**: 「Tailscale 方式からの移行事例」1 件の獲得

---

## 9. 設計判断(旧・未決事項)

以下はインプット資料 §7 の未決事項 8 件に対する決定である。いずれも**推奨案の段階**であり、レビューで覆った場合は本書と関連要求を改訂する。

### 9-1. シリアライズ形式 → **v0.1 は JSON、エンコーダ抽象を切って FlatBuffers へ移行可能にする**

- 決定: FR-G5 のとおりエンコーダを抽象化し、v0.1 の実装は JSON とする。FlatBuffers は性能計測でボトルネックが実証された時点で v0.2 以降に導入する。
- 根拠: (a) JSON はデバッグ容易性(境界を流れるデータを目視できる)と実装速度で圧倒的に有利。(b) 対象アプリは「ロジックが濃く UI が素直」であり、v0.1 のツリー規模で JSON が 60fps を阻害する根拠がまだない。推測でゼロコピー最適化を先行させない。(c) 抽象を先に切っておけば移行コストは限定される。
- 影響: FR-G5、NFR-1(計測項目にエンコード/デコード時間を含める)

### 9-2. イベントペイロードの型表現 → **プリミティブのみで押し切る**

- 決定: イベントは「イベント ID + プリミティブ値(bool / int64 / float64 / string)最大 1 個」で表現する。構造化イベントは導入しない。
- 根拠: v0.1 のイベント 4 種(タップ・テキスト入力・トグル・リスト選択)はすべてこの形に収まる(タップ: 値なし、テキスト: string、トグル: bool、リスト選択: int64)。境界を単純に保つ設計原則(FR-B1/B2)に合致する。
- 影響: 将来ジェスチャ座標等が必要になった場合は複数プリミティブの組を許す拡張で対応し、任意ネスト構造は導入しない。

### 9-3. ナビゲーション/モーダルの所有権 → **Go が単一の情報源 (source of truth)**

- 決定: 画面遷移状態(ナビゲーションパス、モーダル表示フラグ)は Go 側 store が保持する。Swift 側はそれをレンダリングするのみ。ただしスワイプバック・モーダルのプルダウン閉じ等の OS ジェスチャは Swift からイベントとして Go へ通知し、Go が状態を更新して確定させる。
- 根拠: 状態の所有権が分裂すると diff/パッチモデルが破綻する。SDUI の先行知見でも遷移状態のサーバー(=authoring 側)所有が定石。ビジョン(§1.4-1)の「状態管理を Go に集約」に一致する。
- 影響: FR-G2 の NavigationStack 仕様、FR-G6 に「戻る/閉じるイベント」を追加する。

### 9-4. TextField 等の高頻度入力 → **Swift 側ローカル state + コミット時同期**

- 決定: 打鍵中の文字列は Swift 側ローカル state で保持し、フォーカス喪失・確定操作(return キー等)・明示的コミット要求の時点で Go へ同期する。1 打鍵ごとの Go 往復は行わない。Go 側から値を強制上書きするパッチは常に優先される。
- 根拠: 1 打鍵ごとの「境界越え → 再構築 → diff → パッチ → 適用」は日本語 IME の変換中状態と衝突しやすく、60fps 目標(NFR-1)にも不利。入力途中の文字列はビジネスロジック上の意味を持たないことがほとんど。
- 影響: NFR-6 の例外事項として明記。リアルタイムバリデーションが必要なケースは v0.2 で「打鍵デバウンス通知」オプションを検討する。

### 9-5. プレビュー体験 → **v0.1 は macOS ホストによる簡易ループ、ホットリロードはスコープ外**

- 決定: v0.1 では「Go 再ビルド → macOS 上のホストアプリで即確認」の開発ループを提供する(シミュレータ・実機デプロイ不要のループ)。Xcode Previews 統合およびホットリロードは v0.1 スコープ外とし、v0.2 候補とする。
- 根拠: インタープリタ構造上、UI 定義はデータなので将来のホットリロード(バッファ再送)とは相性が良いが、v0.1 で作り込むと本体より先にツールが肥大する。macOS ホストは §9-6 の決定でほぼ無償で手に入る。
- 影響: FR-T3 のサンプルのいずれかを macOS ホストで動くようにする。

### 9-6. macOS を v0.1 に含めるか → **iOS を正式ターゲット、macOS は開発用途のベストエフォート**

- 決定: 正式サポートは iOS 17+ とする。macOS 14+ は SwiftUI 共通 API の範囲で「動作するはず」のベストエフォート扱いとし、CI・受け入れ基準には含めない。
- 根拠: 受け入れ基準(実機動作)とドキュメントの検証コストを iOS に集中させる。一方でインタープリタは SwiftUI 共通 API のみ(NFR-4)なので macOS でもほぼ動き、§9-5 の開発ループに必要。切り捨てず「保証しない」に留めるのが費用対効果最良。
- 影響: NFR-4 の表現(「初期ターゲット iOS 17+ / macOS 14+」のうち保証対象は iOS)。

### 9-7. ライセンスと CLA → **MIT ライセンス、CLA なし(DCO を採用)**

- 決定: ライセンスは MIT。CLA は要求しない。コントリビューションは DCO (Developer Certificate of Origin) の sign-off のみを求める。
- 根拠: 個人発の OSS で CLA は貢献障壁になる。MIT は Go / Swift 両エコシステムで最も摩擦が少なく、比較対象の Govinci も MIT。特許リスクが重大化する規模になった時点で Apache-2.0 への切り替えを再検討すればよい(全コントリビュータ同意が必要になる前の早期判断が前提)。
- 影響: リポジトリに LICENSE, CONTRIBUTING.md(DCO 手順)を含める。

### 9-8. Go 側 API のスタイル → **コンテナは関数合成、modifier はメソッドチェーンのハイブリッド**

- 決定: ツリー構造(親子関係)は可変長引数の関数合成(Govinci 風)、modifier はメソッドチェーン(SwiftUI 風)とする。

  ```go
  VStack(
      Text("Hello").Font(Title).ForegroundColor(Blue),
      Button("Tap", onTap).Padding(8),
  ).Padding(16)
  ```

- 根拠: (a) SwiftUI ユーザーが写経できる見た目になり、学習コストとドキュメント需要が下がる。(b) Go はジェネリクスと戻り値レシーバで fluent API を自然に書ける。(c) 純関数合成のみ(modifier も `Padding(Text(...))` 形式)は Go では括弧のネストが深くなり可読性が落ちる。
- 影響: FR-G1、FR-G3 の API 表面。各コンポーネント型は modifier メソッド後も同一インターフェースを返す設計とする。

---

## 10. リスクと緩和策

| リスク | 緩和策 |
|---|---|
| Swift/SwiftUI の API 変化への追従コスト | インタープリタを薄く保つ(FR-S1, NFR-6)。SwiftUI 安定 API のみ使用(NFR-4) |
| プリミティブ追加要望の際限ない増加 | スコープ外宣言(§2.3)を仕様として明記し、Issue テンプレートから参照する防波堤とする |
| gomobile / cgo ツールチェーンの Xcode バージョン追従 | gomobile 非依存の自前ビルドコマンド(FR-T1)で影響面を限定。CI で最新 Xcode に対する煙テストを行う |
| 競合の出現: Govinci の iOS/SwiftUI 対応(2026-03 時点で活動あり・⭐68) | 差別化点(SwiftUI ネイティブ・C ABI 境界設計・パッチ差分)を README 比較表(FR-T4)で明示。Govinci の diff 設計は研究対象として取り込む(§6) |
| JSON シリアライズが性能目標を満たさない | エンコーダ抽象(FR-G5)により FlatBuffers へ差し替え可能。NFR-1 の計測で早期検知 |

---

## 11. 用語定義

| 用語 | 定義 |
|---|---|
| プロセス内 SDUI | Server-Driven UI のパターンを、ネットワーク越しではなく同一プロセス内の言語間境界(Go ↔ Swift)に適用したもの |
| ノード | UI ツリーの 1 要素。プリミティブ(Text, VStack 等)の種別・プロパティ・子への参照を持つ |
| パッチ | 前回ツリーとの diff から生成される、Swift 側ツリーへの差分適用命令列 |
| インタープリタ | バイトバッファをデコードし、ノード型を SwiftUI View にマップする Swift 側コンポーネント |
| C ABI 境界 | Go(c-archive)と Swift の間で、C の呼び出し規約に落とせる型のみで構成した接合面 |

---

## 12. 変更履歴

| 版 | 日付 | 変更内容 |
|---|---|---|
| 0.1 | 2026-08-27 | 初版ドラフト作成(インプット資料 2026-08 版に基づく) |
